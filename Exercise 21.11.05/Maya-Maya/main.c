#include <stdio.h>
#include <stdlib.h>

int main()
{
    int c = 0;
    int number = 0;

    /*
    for (int digit = 0, mult = 1, isn = 0; (c = getchar ()) != EOF;)
    {
        if (isn)
        {
            if (c == '\n')
            {
                number += digit * mult;
                break;
            }
            else
                isn = 0;
        }

        switch (c)
        {
            case '*':
                if (isn)
                {
                    number += digit * mult;
                    digit = 0;
                    mult *= 20;
                }
                digit ++;
                break;
            case '-':
                digit += 5;
                break;
            case '@':
                number += digit * mult;
                digit = 0;
                mult *= 20;
                break;
            case '\n':
                isn = 1;
                break;
        }
    }
    */

    for (int digit = 0, mult = 1, k = 1; k;)
    {
        k = 0;
        int
        if ()
    }

    printf ("%d", number);
    return 0;
}
